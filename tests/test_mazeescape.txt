[bool rwall, int t] = function exRwall ([int tmp]) begin
rwall <- false
right
if !wall
beginif
rwall <- true
endif
left
end

[bool fwall, int v] = function exFwall ([int ttmp]) begin
fwall <- false
if !wall
beginif
fwall <- true
endif
end

[int tmpp, int tmppp] = function go ([bool r, bool f]) begin
if r && f
beginif
left
endif

if r && !f
beginif
move(1)
endif

if !r
beginif
right
move(1)
endif

end

bool flag = true

for int i = 0:25
beginfor

if (exit && flag)
beginif
move(wall)
flag <- true
endif

if ((!exit) && flag)
beginif
bool rw, int n = exRwall[1]
bool fw, int m = exFwall[1]
int z, int x = go[rw, fw]
endif

endfor
